import { useEffect, useState } from "react"
import DoJob from "./DoJob"

export default function Main(props){
    
const styles={
    background:props.dark?"white":"  hsl(235, 21%, 11%)"
}


    return(
        <main style={styles} >
            {/* {props.arrOfJobsLength} */}
        </main>
    )
}